when:
  - event: tag        # chỉ build khi push TAG

steps:
  build-push:
    image: woodpeckerci/plugin-docker-buildx
    settings:
      repo: tuyentvwork/railskylines-be
      tags:
        - ${CI_COMMIT_TAG}    # image tag = tên git tag
        - latest
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
